-- Select and display the score and name columns from second_table
-- where score is greater than or equal to 10
-- and order the results by score in descending order
SELECT score, name
FROM second_table
WHERE score >= 10
ORDER BY score DESC;
